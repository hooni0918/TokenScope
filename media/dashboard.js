// Dashboard client-side script
// Currently static rendering; will support interactive filtering in future updates.

(function () {
  const vscode = acquireVsCodeApi();

  // Placeholder for future interactivity (date filtering, export, etc.)
  console.log('TokenScope Dashboard loaded');
})();
