{
  "name": "tokenscope",
  "displayName": "TokenScope",
  "description": "Auto-track AI coding tool token usage per project. Supports Claude Code and OpenAI Codex CLI. See how many tokens you've spent on each workspace.",
  "version": "0.5.0",
  "publisher": "hooni",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hooni0918/TokenScope"
  },
  "icon": "resources/icon.png",
  "galleryBanner": {
    "color": "#1a1a2e",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "keywords": [
    "claude",
    "claude code",
    "codex",
    "openai",
    "token",
    "usage",
    "tracking",
    "cost",
    "LLM",
    "anthropic"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tokenScope.showDashboard",
        "title": "Show Token Dashboard",
        "category": "TokenScope"
      },
      {
        "command": "tokenScope.exportCsv",
        "title": "Export Usage to CSV",
        "category": "TokenScope"
      },
      {
        "command": "tokenScope.labelSession",
        "title": "Label Session",
        "category": "TokenScope",
        "icon": "$(tag)"
      },
      {
        "command": "tokenScope.compareSessions",
        "title": "Compare Sessions",
        "category": "TokenScope",
        "icon": "$(diff)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "tokenScope.compareSessions",
          "when": "view == tokenScope.usageView",
          "group": "navigation@1"
        }
      ],
      "view/item/context": [
        {
          "command": "tokenScope.labelSession",
          "when": "view == tokenScope.usageView && viewItem == tokenScope.session",
          "group": "inline"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "tokenscope",
          "title": "TokenScope",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "tokenscope": [
        {
          "id": "tokenScope.usageView",
          "name": "Token Usage"
        }
      ]
    },
    "configuration": {
      "title": "TokenScope",
      "properties": {
        "tokenScope.sessionLimit": {
          "type": "number",
          "default": 50,
          "minimum": 5,
          "maximum": 500,
          "description": "Maximum number of sessions to display in the tree view and dashboard."
        },
        "tokenScope.showCostEstimate": {
          "type": "boolean",
          "default": true,
          "description": "Show estimated cost based on Claude model pricing."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.1",
    "typescript": "^5.3.0"
  }
}
